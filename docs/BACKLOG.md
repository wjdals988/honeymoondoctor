# 개선 백로그 (v0.2.1 기준, 2026-08-12 갱신)

> 2026-08-12: v0.1.5~v0.2.1에서 완료된 항목을 ~~취소선~~으로 표시하고, 재검증에서
> 사실이 아닌 것으로 확인된 2건(1-10, 3-2j)을 정정했다.

실기기 QA와 코드 확인으로 뽑은 목록이다. 각 항목은 **무엇 / 왜 / 어디 / 규모**로 적었고,
"확인함"은 이 저장소 코드나 실기기에서 직접 본 것, "추정"은 근거 기반 판단이다.

규모 기준: **S** 반나절 이내 · **M** 1~3일 · **L** 1주 이상.

---

## 1. 기능 구멍 — 쓰다 보면 막히는 것

| # | 항목 | 왜 문제인가 | 어디 | 규모 |
|---|---|---|---|---|
| ~~1-1~~ | ~~여행을 하나만 가질 수 있다~~ **v0.1.6 완료** — 여행 목록·전환·다중 여행 | `observeMyTrip(uid): Flow<Trip?>` 구조라 한 계정이 여행 1개만 갖는다. 두 번째 여행을 만들려면 기존 여행을 지워야 하고, 지난 여행을 다시 볼 방법이 없다. 여행 앱에서 "지난 여행 보기"가 없는 건 치명적이다. | `domain/repository/TripRepository.kt`, `AuthGateViewModel` | **L** |
| ~~1-2~~ | ~~여행 삭제 UI가 없다~~ **v0.1.5 완료** — 여행 삭제·나가기 | 삭제는 회원 탈퇴 흐름 안에만 있다. 잘못 만든 여행을 지우려면 계정을 탈퇴해야 한다. | `feature/tripinfo`, `DeleteAccountUseCase` | S |
| ~~1-3~~ | ~~도시 삭제가 없다~~ **v0.6.0 완료** — 참조(일정·경비·장소) 해제 후 삭제, 영향 건수 사전 안내 | `CityRepository`에 `delete`가 아예 없다(확인함). 오타로 만든 도시가 영구히 남는다. 일정·경비·장소가 그 도시를 참조하므로 참조 정리 규칙을 먼저 정해야 한다. | `domain/repository/CityRepository.kt` | M |
| ~~1-4~~ | ~~검색이 없다~~ **v0.4.2 완료** — 장소(이름·메모)·예약(예약명·업체·예약번호)·준비물(이름) 인라인 검색. 일정·경비는 기존 오버뷰·필터로 충분 | 일정·예약·경비·장소 어디에도 검색이 없다(확인함). 항목이 수십 개가 되면 스크롤로만 찾아야 한다. | 전 화면 | M |
| 1-5 | **사진 첨부가 없다** | 여행 일기를 자칭하는 앱에 사진이 없다(확인함). 바우처만 기기 내 파일로 붙는다. Firebase Storage를 쓰면 무료 티어·용량 정책을 함께 정해야 한다. | 신규 | L |
| ~~1-6~~ | ~~통화가 KRW·EUR·CZK 3종 고정~~ **v0.2.0 완료** — 18종 + 환율 자동 조회·기억 | `enum class TravelCurrency`에 하드코딩(확인함). 동남아·미국·일본 여행에 못 쓴다. 환율도 사용자가 직접 입력한다. | `domain/model/Expense.kt` | M |
| ~~1-7~~ | ~~준비물 기한(dueAt)이 모델에만 있다~~ **v0.2.1 완료** — 출발일 기준 프리셋 칩 + 기한 지남 표시 | 필드는 있는데 입력 UI가 없다(확인함). v0.1.3에서 고친 "도시 체류 날짜"와 똑같은 형태의 죽은 코드다. | `feature/checklist` | S |
| ~~1-8~~ | ~~이동 일정의 출발 권장 시각 미구현~~ **v0.3.0 완료** — 설정의 여유(기본 60분)를 뺀 권장 시각, 지나면 경고. 지도 이동시간 미반영은 알려진 한계 | 스펙 항목인데 계산기가 없다(확인함). "공항까지 1시간, 지금 나가야 함" 같은 알림이 이 앱의 핵심 가치와 가장 맞닿아 있다. | 신규 usecase | M |
| ~~1-9(설정)~~ | ~~설정 화면이 메뉴만 있다~~ **v0.3.0 완료** — 테마·출발 여유. 긴급상황 화면은 여전히 자리표시자 | "추후 단계에서 제공"으로 표시된다. 설정이 없어 알림 시각·기본 통화·테마를 바꿀 수 없다. | `feature/more` | M |
| ~~1-10~~ | ~~경비 삭제 시 역방향 참조 정리 누락~~ **정정(2026-08-12): 사실 아님.** 참조는 지출→일정/예약 단방향이고 역방향 필드가 어떤 모델에도 없다(`expenseId` 전수 검색으로 확인). 지출을 지워도 매달린 참조가 생기지 않는다. | — | — |
| ~~1-11~~ | ~~전체 데이터 내보내기가 장소에만 있다~~ **v0.6.0 완료** — 여행 전체 JSON 백업(formatVersion 1). 가져오기(복원)는 미구현 | 여행 기록을 통째로 백업할 방법이 없다. 계정을 잃으면 전부 사라진다. | `feature/nearby/PlaceImport*` 확장 | M |
| ~~1-12~~ | ~~일정을 지도로 볼 수 없다~~ **v0.6.9 완료** — 일정 탭에 목록/지도 토글 추가. 저장된 장소(주변 탭)를 일정에 연결(`ItineraryItem.placeId`)하면 그날 방문 순서대로 번호 핀(1,2,3...)이 지도에 찍힌다. 미연결 일정은 "위치 미확인 N건"으로 안내 | "급할 때는 지도랑 일정만 보고 움직여야 한다"는 사용자 요청. 일정에 좌표가 전혀 없어(`location`/`address`는 자유 텍스트) 지도 마킹이 불가능했다. | `domain/usecase/ItineraryDayStops.kt`, `feature/itinerary/ItineraryScreen.kt`, `core/ui/OsmMiniMap.kt` | M |

## 2. 일반 사용자에게 팔려면 반드시 필요한 것

| # | 항목 | 왜 필요한가 | 규모 |
|---|---|---|---|
| ~~2-1~~ | ~~앱 내 업데이트 확인~~ **v0.1.5 완료** — GitHub Releases 조회 | 스토어를 거치지 않는 직접 APK 배포라, 새 버전이 나와도 사용자가 알 방법이 없다. 이번에 넣은 버전 정보 화면은 "지금 버전"만 보여준다. GitHub Releases API로 최신 버전을 조회해 알려주는 것만으로 충분하다. **이게 없으면 크래시를 고쳐도 사용자에게 도달하지 않는다.** | S |
| 2-2 | **크래시 리포팅이 없다** | Crashlytics 같은 게 없다(확인함). 이번 크래시도 제가 우연히 재현해 찾았다. 실사용자가 겪는 크래시는 아무도 모른다. 다만 SPEC의 "분석 SDK 미포함" 원칙과 충돌하므로 정책 결정이 먼저다. | S |
| 2-3 | **개인정보처리방침·이용약관 공개 URL** | 문서는 `docs/`에 초안이 있지만 게시되지 않았다. 스토어 심사에도, 일반 배포에도 공개 URL이 필요하다. | S(사용자 결정) |
| 2-4 | **다국어 미지원** | 리소스 폴더가 `values` 하나뿐이고 코드에 한국어 문자열이 약 406곳 박혀 있다(확인함, `strings.xml`은 9개뿐). 해외 사용자는 물론, 스토어 글로벌 노출도 불가능하다. 문자열 추출부터가 큰 작업이다. | **L** |
| 2-5 | **2인 고정 구조** | Firestore 규칙에 `memberIds.size() <= 2`가 박혀 있다. 가족·친구 3~4인 여행으로 시장을 넓히려면 규칙·UI·정산 로직(1/2 고정)을 함께 바꿔야 한다. 반대로 "둘이서"를 브랜드로 밀면 그대로 두는 게 맞다. **먼저 결정할 사업 판단.** | L |
| 2-6 | **Firestore 요금제** | Spark(무료) 기준이라 사용자가 늘면 읽기·쓰기 한도를 넘긴다. 홈 화면이 8개 컬렉션을 실시간 구독하는 구조여서 사용자당 읽기 수가 적지 않다. 유료 전환 시점과 비용 상한을 미리 정해둬야 한다. | 사용자 결정 |
| ~~2-7(시작)~~ | ~~UI 자동화 테스트 0개~~ **v0.3.1 시작** — 공용 컴포넌트(칩·별점·되돌리기·접기) Compose 테스트 8건, 에뮬레이터 통과. 화면 단위(Hilt) 테스트는 남음 | `app/src/androidTest`에 파일이 하나도 없다(확인함). 유닛테스트 73개는 계산 로직만 덮는다. 화면이 20개가 넘는데 회귀는 매번 수동이다. | M |
| 2-8 | **수익 모델·스토어 제출** | 무료/광고/구독 중 무엇도 결정되지 않았다. 유료화하면 사업자등록·세무가 따라온다. | 사용자 결정 |

## 3. UI / UX 개선

### 3-1. 지금 바로 걸리는 것

| # | 항목 | 근거 |
|---|---|---|
| ~~a~~ | ~~"여행 완료 처리"에 확인 대화상자가 없다~~ **v0.1.5 완료** | 한 번의 탭으로 전체가 읽기전용이 된다. 되돌릴 수 있지만, 되돌리는 방법을 모르면 앱이 고장 난 것처럼 보인다. 실기기에서 확인함. |
| ~~b~~ | ~~읽기전용 상태를 각 탭에서 알 수 없다~~ **v0.1.5 완료** — 전 탭 상단 띠 | v0.1.2에서 쓰기 버튼을 감췄더니, 이제는 버튼이 왜 사라졌는지 알 수 없다. 완료된 여행에서는 각 탭 상단에 "완료된 여행 — 읽기 전용" 띠가 필요하다. |
| ~~c~~ | ~~빈 상태가 안내만 하고 행동을 주지 않는다~~ **v0.2.1 완료** — 지출·장소·준비물·결정함 빈 화면에 추가 버튼 | "저장된 장소가 없습니다"에서 바로 추가할 버튼이 없다. `EmptyState`에 `action` 슬롯이 이미 있는데 대부분 안 쓴다. |
| ~~d~~ | ~~날짜·시간 입력이 다이얼로그 연속 탭이다~~ **v0.2.2~0.2.3 완료** — 지출 오늘/어제 칩, 일정 시각 프리셋 칩, 준비물 기한 프리셋 칩 | 일정 하나 넣는 데 날짜 → 확인 → 시각 → 확인으로 4탭이다. 자주 쓰는 값(현재 시각 기준 30분 뒤 등) 프리셋 칩이 있으면 크게 줄어든다. |
| ~~e~~ | ~~삭제에 되돌리기가 없다~~ **v0.2.1 완료** — 지출·일정·장소·준비물 삭제 후 되돌리기 스낵바(Material 3 undo 패턴) | 확인 대화상자만 있고 실행 후 취소가 불가능하다. 스낵바 "되돌리기"가 표준이다. |
| ~~f~~ | ~~로딩이 전면 스피너다~~ **v0.6.6 완료** — 홈·일정·주변·경비·예산·예약함·준비물·결정함 8개 화면의 최초 로딩을 각 화면 모양을 흉내낸 스켈레톤(shimmer)으로 교체 | 화면 전체가 비었다가 한꺼번에 채워진다. 스켈레톤이면 깜빡임이 줄어든다. |
| ~~g~~ | ~~홈 카드 순서가 고정~~ **정정(2026-08-13): 문제 없음.** 코드 확인 결과 여행 중에는 `NextItineraryCard`가 이미 맨 위로 오고, 현지 시각도 이미 헤더에 항상 표시되고 있어 이전 단계에서 이미 해소된 항목이었다(3-2j와 같은 패턴). |

### 3-2. 접근성 · 기기 대응

| # | 항목 | 근거 |
|---|---|---|
| h | **가로 모드·태블릿 대응이 없다** | 매니페스트에 방향 고정도, `WindowSizeClass` 분기도 없다(확인함). 태블릿에서 한 컬럼으로 늘어난다. |
| ~~i~~ | ~~큰 글자 크기 미검증~~ **v0.6.3 완료** — 실기기에서 시스템 글자 크기 1.3배로 실측, 발견된 클리핑 버그 2건(홈 헤더 "여행 바꾸기", 예약 상세 바우처 파일명) 수정 | 시스템 글자 크기를 키웠을 때 카드가 깨지는지 확인하지 않았다. `AppCard` 안의 `Row`들이 한 줄 가정으로 짜여 있다. |
| ~~j~~ | ~~아이콘 설명 누락 6곳~~ **정정(2026-08-12): 문제 없음.** null 7곳을 전수 확인한 결과 모두 텍스트 옆 장식 아이콘이었고, 아이콘만 있는 IconButton에는 누락이 없다. 장식 아이콘의 null은 TalkBack 중복 낭독을 막는 올바른 값이다. |
| ~~k~~ | ~~색만으로 상태를 구분하는 곳~~ **v0.6.1 부분 완료** — 홈 "N시간 남음"에 긴급도 아이콘 추가. 예약 상태·일정 충돌 경고는 이미 텍스트 라벨을 병기하고 있어 재검증 결과 해당 없음(3-2j와 같은 패턴) |

### 3-3. 완성도(팔려면 필요한 수준)

| # | 항목 |
|---|---|
| ~~l~~ | ~~첫 실행 온보딩이 없다~~ **v0.6.7 완료** — 로그인 전 3장짜리 온보딩(이모지 히어로 + 헤드라인 + 설명, 스와이프/버튼 전환), DataStore로 1회만 표시 | 로그인하면 곧바로 여행 생성 폼이다. 이 앱이 무엇을 해주는지 설명하는 화면 2~3장. |
| m | **앱 아이콘·스크린샷·스토어 문구** **v0.6.8 부분 완료** — 아이콘에 대각선 그라디언트로 입체감 추가, 기능 스크린샷 7장(홈/일정/경비/주변/전체/준비물/로그인) 촬영, 스토어 짧은/긴 설명 문구 초안 작성(스크래치패드). **미완**: 아이콘은 여전히 도형 조합 수준(전문 디자이너 영역), 스크린샷도 기기 프레임·캡션을 얹은 마케팅용이 아니라 기능 확인용이다. | 아이콘은 도형 조합 수준이다. 팔려면 전문 아이콘과 스토어 스크린샷이 필요하다. |
| ~~n~~ | ~~애니메이션·전환 효과가 기본값~~ **v0.6.8 완료** — 화면 전환에 M3 페이드 스루(페이드+스케일) 적용, 일정·주변·경비·준비물·결정함·예약함 목록의 추가/삭제/재정렬에 `animateItem()` 애니메이션 적용 | 화면 전환, 카드 등장에 모션이 없어 정적으로 느껴진다. |
| ~~o~~ | ~~햅틱 피드백 없음~~ **v0.6.2 완료** — 준비물 체크, 지출·일정·장소·예약 저장, 5종 삭제 확인에 촉각 피드백 |
| ~~p~~ | ~~다크/라이트 모드 수동 선택 불가~~ **v0.3.0 완료** — 설정 → 테마 — 시스템 설정만 따른다(설정 화면이 없어서). |

## 4. 아직 검증하지 못한 것 (리스크)

> **2026-08-12 검증 라운드 결과** (에뮬레이터 데모 모드 + 실기기):
>
> - ~~4-3 알림 실발사~~ **검증 완료 + 버그 발견·수정.** 이동 일정을 만들자
>   ItineraryReminderWorker가 WorkManager에 등록되고 목표 시각(18:50:00)에 정확히
>   실행돼 "곧 일정: 기차 이동" 알림이 게시됐다(18:50:26 확인). 단, **신규 설치에서는
>   앱이 POST_NOTIFICATIONS 권한을 요청하는 곳이 동기화 상태 화면뿐이라 알림이 조용히
>   버려졌다**(importance=NONE 실측) — v0.3.1에서 일정 탭 최초 진입 시 요청하도록 수정.
>   재부팅 후 재예약은 미검증으로 남음.
> - ~~4-4 바우처 첨부~~ **검증 완료.** PDF 푸시 → 예약 상세 → 바우처 추가 → SAF 선택 →
>   "기기 내 바우처" 목록에 표시까지 확인.
> - 4-5 오프라인 큐 — **부분 검증.** 비행기 모드에서 지출 저장 → 해제 → "동기화 대기
>   0건·온라인"으로 정상 반영 확인(happy path). **거부 케이스는 UI로 재현 불가** —
>   규칙 위반 쓰기는 UI가 전부 막고 있어, 유일한 거부 경로는 4-1의 경합뿐이다.
> - 4-1 경합 — **양쪽 반을 분리 검증(2026-08-12 후속).** 기기 2대 E2E는 서브계정이
>   없어 불가하지만, 경합의 실체는 두 반쪽이다: ① 서버가 완료된 여행 쓰기를 거부 —
>   규칙 테스트 "완료된 여행은 구성원도 하위 컬렉션을 쓸 수 없다" 통과(36/36),
>   ② 클라이언트가 거부를 크래시 없이 안내로 변환 — ActionErrorStateTest가
>   PERMISSION_DENIED → 한국어 메시지 경로를 커버하고, 실패한 삭제에는 되돌리기도
>   내밀지 않는다(runReporting=false 분기). 남은 것은 SDK 오프라인 큐 재생 타이밍의
>   실측뿐이며 SDK 계약상 같은 예외 경로다.
> - 4-2 탈퇴 실행 — 규칙 반쪽(자기 탈퇴·소유권 이전 6건)은 규칙 테스트 통과.
>   Kotlin 삭제 로직의 실행은 실계정이 필요해 미검증(사용자 결정 대기).
> - ~~4-6 대량 데이터~~ **부분 검증.** 에뮬레이터 데모에서 지출 30건 생성 후 측정:
>   경비 목록 스크롤 잰크 5.5%(507프레임 중 28), 홈 4.8%, 95th 34ms — 이 규모에서는
>   문제 없음(에뮬레이터 SW 렌더링 기준이라 실기기는 더 여유). 수백 건은 미검증.

| # | 항목 | 왜 남았나 |
|---|---|---|
| 4-1 | **파트너가 먼저 여행을 완료 처리한 순간의 경합** | 기기 2대가 필요하다. v0.1.2의 예외 처리가 실제로 이 경로를 막는지는 유닛테스트로만 확인했다. |
| 4-2 | **회원 탈퇴 실제 실행** | 되돌릴 수 없는 삭제라 보류했다. 3가지 케이스(소유자 단독/소유자+동반자/일반 구성원) 모두 미실행. |
| 4-3 | **로컬 알림 실발사, 재부팅 후 재예약** | 24/3/1시간 전 알림이 실제로 뜨는지 확인하지 않았다. |
| 4-4 | **바우처 파일 실제 첨부** | 파일 선택 → 저장 → 열기 경로 미검증. |
| 4-5 | **오프라인 큐가 나중에 거부당하는 경우** | 비행기 모드에서 쓰고 온라인 복귀 시 규칙 위반으로 거부되면 어떻게 보이는지 미확인. |
| 4-6 | **데이터가 많을 때의 성능** | 항목 수십~수백 개 상태를 만들어본 적이 없다. 홈이 8개 컬렉션을 동시 구독한다. |

## 5. 권고 순서

**1순위(팔기 전에 반드시)** — 2-1 업데이트 확인, 3-1a 완료 처리 확인, 3-1b 읽기전용 띠,
1-2 여행 삭제. 넷 다 규모 S이고, 없으면 "고장 난 앱"으로 보이거나 수정이 사용자에게
도달하지 못한다.

**2순위(제품이 되려면)** — 1-1 여행 여러 개, 1-8 출발 권장 시각, 1-9 설정 화면,
2-7 UI 자동화 테스트. 1-1은 L이지만 이게 없으면 "한 번 쓰고 끝나는 앱"이다.

**3순위(시장 확대)** — 2-4 다국어, 1-6 통화 확장, 1-5 사진, 2-5 인원 제한 완화.
2-5는 구현 전에 "둘이서"를 브랜드로 유지할지 결정하는 게 먼저다.

**병행** — 4장의 미검증 항목은 기기 2대·시간 조작 등 준비가 필요하므로, 기능 작업과
별개로 한 번에 몰아서 검증하는 게 효율적이다.
