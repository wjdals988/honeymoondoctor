<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 화면 진입/Pull-to-Refresh 시점에만 위치를 취득한다. 백그라운드 위치 권한은 요청하지 않는다. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- 중요 일정 알림의 정시성을 위해 요청하되, 거부 시 WorkManager 비정확 알림으로 대체한다. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <!-- AlarmManager로 예약한 정확한 알람은 재부팅 시 사라지므로, 부팅 완료 시 다시 계획한다. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".HoneymoonDoctorApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.HoneymoonDoctor"
        android:localeConfig="@xml/locales_config">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.HoneymoonDoctor"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 바우처 파일을 기기 내부 저장소에서 FileProvider로만 연다. 외부 앱과 파일 시스템 경로를 직접 공유하지 않는다. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

        <!-- 정확한 알람 발사 지점. 앱 내부에서 발급한 PendingIntent로만 호출되므로 비노출. -->
        <receiver
            android:name=".core.notification.ItineraryAlarmReceiver"
            android:exported="false" />

        <!-- 재부팅 후 정확한 알람 재예약 전용. 시스템 보호 브로드캐스트만 받는다. -->
        <receiver
            android:name=".core.notification.BootCompletedReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
